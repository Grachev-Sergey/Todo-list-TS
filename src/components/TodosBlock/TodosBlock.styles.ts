import styled from 'styled-components'

export const TodosContainer = styled.section`
  max-width: 550px;
  background: ${props => props.theme.colors.generalBg};
  box-shadow: 0 2px 4px 0 rgb(0 0 0 / 20%), 0 25px 50px 0 rgb(0 0 0 / 10%);
  margin: 0 auto;
`