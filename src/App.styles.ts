import styled from 'styled-components';

export const AppContainer = styled.div`
  box-sizing: border-box;
  margin: 0 15px;
  padding: 0;
  background-color: ${props => props.theme.colors.generalBg};
`